#include<stdio.h>
void main()
{
	int a = 3, b = 5;
	//a = a + b;a为8，b为5
	//b = a - b;a为8，b为3
	//a = a - b;a为5，b为3，代码完成,这个方法不适用与数字太大的方式因为会数据溢出
	a = a ^ b;//011^101==110
	b = a ^ b;//110^101=011
	a = a ^ b;//110^011=101
}